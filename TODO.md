# TODO

- [ ] Snapshot metrics automatically at the end of every rebuild (`engine/run`), so history accrues without an operator action. Today `POST /api/metrics/snapshots` is the only writer; series charts and trend goals (`not enough history to judge a trend`) stay empty until someone calls it. The console's "Snapshot now" button was removed pending this.
- [ ] Enrich automatically at the end of every rebuild (`engine/run`), right after the metrics snapshot: read every eligible entity not yet read under the current vocabulary (`enrichment/store.enrich`), so transcripts get their labels without an operator action; when `ENRICHMENT_ENABLED` is off the step is a no-op. Today `POST /api/enrichment/run` is the only trigger. The console's Run / force / limit controls were removed pending this.
- [ ] Rebuild at the end of every sync run (`sync.run_all`, then `engine/run`): exactly one rebuild per run, not one per source, so Sync all's 27 sources end in a single recompute. If a rebuild is already in progress, mark one as pending and run it when the current one finishes rather than dropping it. The rebuild's outcome must be visible without anyone clicking: the Rebuild tab's status line and the health pill should show a failed automatic rebuild. Today `POST /api/rebuild` is only ever called by hand, so new data sits in `raw_event` until someone remembers.
- [ ] Sync every enabled source on a schedule (`sync.run_all` with the stored switches honoured), so data arrives without an operator pressing Sync all. With the item above, each scheduled run ends in a rebuild, then the metrics snapshot, then enrichment. The console's Sync all and per-source Sync buttons stay until this lands.
- [ ] Rebuild when a knowledge file changes (`knowledge/*.yaml`, `knowledge/briefs/*.md`), since new mappings, rules or goals need a recompute with no new data at all. Until then the console's Rebuild button stays for exactly that case.
