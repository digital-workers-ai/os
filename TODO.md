# TODO

- [ ] Snapshot metrics automatically at the end of every rebuild (`engine/run`), so history accrues without an operator action. Today `POST /api/metrics/snapshots` is the only writer; series charts and trend goals (`not enough history to judge a trend`) stay empty until someone calls it. The console's "Snapshot now" button was removed pending this.
- [ ] Enrich automatically at the end of every rebuild (`engine/run`), right after the metrics snapshot: read every eligible entity not yet read under the current vocabulary (`enrichment/store.enrich`), so transcripts get their labels without an operator action; when `ENRICHMENT_ENABLED` is off the step is a no-op. Today `POST /api/enrichment/run` is the only trigger. The console's Run / force / limit controls were removed pending this.
- [ ] Rebuild at the end of every sync run (`sync.run_all`, then `engine/run`): exactly one rebuild per run, not one per source, so Sync all's 27 sources end in a single recompute. If a rebuild is already in progress, mark one as pending and run it when the current one finishes rather than dropping it. The rebuild's outcome must be visible without anyone clicking: the Rebuild tab's status line and the health pill should show a failed automatic rebuild. Today `POST /api/rebuild` is only ever called by hand, so new data sits in `raw_event` until someone remembers. A failed rebuild writes no `engine_run` row at all today (`engine/run` only ever constructs `ok=True`), so the Rebuild tab's history can only show successes; write the failed row with the failure under `report["error"]`, which is the key the runs endpoint already reads.
- [ ] Sync every enabled source on a schedule (`sync.run_all` with the stored switches honoured), so data arrives without an operator pressing Sync all. With the item above, each scheduled run ends in a rebuild, then the metrics snapshot, then enrichment. The console's Sync all and per-source Sync buttons stay until this lands.
- [ ] Rebuild when a definition file changes (`definitions/*.yaml`, `definitions/briefs/*.md`), since new mappings, rules or goals need a recompute with no new data at all. Until then the console's Rebuild button stays for exactly that case.
- [ ] OAuth 2.1 with PKCE on `/mcp` (and a token on `/api`), so the MCP server can be reached from outside the machine. Today the whole API is open and the MCP server is documented as localhost only; the 2026 spec makes OAuth mandatory for a remote Streamable HTTP server and forbids passing a client's token through.
- [ ] MCP as a source connector: a source with `transport: mcp` and a server URL, whose connector discovers the server's tools, calls its list or search tools and lands results as raw events that `mappings.yaml` maps like any other source, so a provider that ships its own MCP server (HubSpot's went GA in April 2026) needs no bespoke client. Needs client-side OAuth, per-server pagination semantics and a mock MCP server for the fixtures.
- [ ] Search over ask conversations once an Ask page exists (`conversation_thread`, `conversation_turn`): every question and answer is stored, but no page shows them, so a search hit would have nowhere to open. Index them as `search_document` rows with kind `conversation` and land on the thread with the turn selected, the same way briefings open `/ai/coaching?briefing=<seq>`.
- [ ] `search` as an ask-agent tool and an MCP tool: one entry in `conversation/agent.py` `TOOLS`/`HANDLERS` calling `engine/search.query`, which the MCP server exposes and logs on its own. Returns `kind, id, label, evidence` (evidence under 200 characters), `limit` default 10 clamped at 20, no `href`; the description partitions it from `find_entities` (an exact value you already hold vs words, partial names, topics); `PROMPT_VERSION` bumps; the MCP tests' tool counts and the README's "six tools" update. Coaching and enrichment stay single-shot. Demo: asking "which subscriptions are past due?" produces a `search` tool receipt, and `GET /api/mcp` lists `search`.
